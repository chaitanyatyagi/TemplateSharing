DATABASE=mongodb+srv://chaitanya1540:chaitanya1540@cluster0.xkduy.mongodb.net/AMIT-Dev

# --- Email (purchase confirmation + template delivery) ---
# EMAIL_USER: the Gmail address that sends the mail
# EMAIL_PASS: a Gmail *App Password* (16 chars, NOT your normal password).
#             Generate at https://myaccount.google.com/apppasswords
#             (requires 2-Step Verification enabled on the account).
# EMAIL_FROM: optional display From (defaults to EMAIL_USER).
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
